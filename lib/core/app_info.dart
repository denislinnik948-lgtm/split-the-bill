/// Brand/display name shown across the app and used as the window title.
/// Not localized — the product name is the same in every language.
const String kAppName = 'Split the Bill';

/// User-facing version, shown on the Information screen.
const String kAppVersion = '1.0.1';

/// Support/contact address shown in the privacy policy.
/// TODO: replace with a real address before publishing.
const String kSupportEmail = 'support@splitthebill.app';
